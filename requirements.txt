fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
# Direct bcrypt, not passlib[bcrypt]: passlib's bcrypt backend detection is broken against
# bcrypt>=4.0 (it probes `bcrypt.__about__`, removed upstream) — hashpw/checkpw directly
# needs no compatibility shim and does exactly what this app needs.
bcrypt>=4.0,<6.0
itsdangerous>=2.1,<3.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0
httpx>=0.27,<1.0
openpyxl>=3.1,<4.0
pytest>=8.0,<9.0
